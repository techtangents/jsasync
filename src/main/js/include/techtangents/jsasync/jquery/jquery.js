Ephox.core.module.define("techtangents.jsasync.jquery", [], function(api) {

    var Libraries = techtangents.jsasync.glue.Libraries;

    /** ajax :; AjaxOptions -> Bfuture AjaxPass AjaxFail */
    var ajax = function(url) {


    };

    api.ajax = ajax;
});